# Health Tracker

Start the dev server: `./mvnw spring-boot:run -Dspring-boot.run.arguments=--server.port=3103` — in a second terminal, run `./watch-static.sh` to mirror `static/` edits into `target/classes` so spring-boot-devtools live-reloads the browser.
